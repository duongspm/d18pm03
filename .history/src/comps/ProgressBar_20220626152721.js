import React from 'react';
import useStorage from '../hooks/useStorage';
const ProgressBar = () => {
    return (
        <div className='pro'>
            
        </div>
    );
};
import useStorage from '../hooks/useStorage';
export default ProgressBar;